# Dependencies and build output
node_modules/
dist/
build/
coverage/
.vite/
.wrangler/
*.tsbuildinfo

# Secrets: never commit real env files
.env
.env.*
!.env.example

# Swarm runtime state (binary DBs, pids, logs).
# Durable snapshots are exported to docs/swarm/ instead.
.claude-flow/
.swarm/
ruvector.db
*.db-shm
*.db-wal

# OS / editor noise
.DS_Store
*.log
.idea/
.vscode/

# Browser check screenshots and downloads
tests/starter-e2e/shots/
